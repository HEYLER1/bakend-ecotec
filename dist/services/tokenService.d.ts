/**
 * Servicio para manejo de tokens JWT
 * Genera y gestiona access tokens y refresh tokens
 */
export declare const generateTokens: (userId: number, username: string) => {
    accessToken: string;
    refreshToken: string;
};
//# sourceMappingURL=tokenService.d.ts.map